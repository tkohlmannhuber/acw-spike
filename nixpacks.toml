[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["rm -f package-lock.json && npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run db:migrate && node .output/server/index.mjs"
